---
name: export
description: Export token-burningman data as JSON or CSV.
---

Read session files from `~/.token-burningman/sessions/*.jsonl` or hourly files from `~/.token-burningman/hourly/*.json`. Default: JSON, 7d, sessions. Output to stdout. For CSV, flatten fields and add headers.
